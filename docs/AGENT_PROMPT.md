# Setup Prompt for Implementation Agent

Use the following prompt to set up the SpendSense project structure according to the PRDs:

---

**Setup the SpendSense pre-implementation structure by following the specifications in `IMPLEMENTATION_PROMPT.md`. This prompt is based on `SS_Reqs_PRD.md` and `SS_Architecture_PRD.md`, and ensures the folder structure, configuration files, and setup match the design, architecture, and layout defined in those PRDs precisely. Cross-reference `IMPLEMENTATION_SETUP.md` and `PROJECT_STRUCTURE.md` for additional implementation details, but always prioritize the PRD specifications as the authoritative source.**

---

Then provide the full content from `IMPLEMENTATION_PROMPT.md`:
